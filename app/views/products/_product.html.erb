<div class="col-sm-2 col-md-4">
  <h1><%= product.name %></h1>
  <%= image_tag(product.image_url, width: "400", height: "350") if product.image_url%>
  <p>Colour: <%= product.colour %></p>
  <p>Description: <%= product.description %></p>
  <p>Price: $<%= product.price %></p>
</div><!-- /col -->
